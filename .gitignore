# Build output
dist/
*.tsbuildinfo

# Node modules
node_modules/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS-specific
.DS_Store
Thumbs.db

# Editor directories and files
.vscode/
.idea/
*.sw?

# Environment files (if any)
.env
.env.local
.env.*.local

# Optional: test coverage (if you add tests later)
coverage/

# Optional: lockfiles if you don’t want to commit them (if using multiple managers)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Optional: cache files if you don’t want to commit them
# .next/cache